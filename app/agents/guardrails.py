"""Input guardrails: prompt-injection screening run before the router agent processes a query."""
# TODO(M8): SDK input guardrail with tripwire on injection attempts (renumbered from M7:
# the plan now bundles guardrails with MCP + Slack in M8; M7 became the AWS migration)
