# CLAUDE.md — agentdesk

Working brief for Claude Code. Read this first each session, then `README.md` (full spec) and `DECISIONS.md` (ADRs with rationale).

## What this is

An AI-powered ITSM service desk — a **portfolio project**. A tool-less **router agent** classifies each query and hands off to one of three specialists:

- **knowledge** — query expansion, hybrid RAG (pgvector + FTS, reciprocal rank fusion), citations
- **fulfillment** — reads user assets, pre-fills catalog orders, human-in-the-loop approval for orders > $500
- **incident** — summarize, dedup via ticket embedding similarity, create/link tickets

**Stack:** OpenAI Agents SDK (models via LiteLLM) · FastAPI · Streamlit · Postgres+pgvector (relational + vector + FTS in one DB) · Redis (embedding/semantic/response caches) · Langfuse · custom eval harness in GitHub Actions · MCP server + Slack Socket Mode (M8) · optional Neo4j Graph-RAG backend (M9).

**Key invariants (see DECISIONS.md):** tools are the *only* DB access path; router carries no tools; semantic cache runs before any agent and only for read-only intents; short-term memory = SDK sessions in Postgres, long-term = hand-rolled `user_facts` (inject at session start, extract at end).

## Current status (as of 2026-07-12)

**THE PROJECT IS COMPLETE** (core scope; post-completion milestones M10 latency ✅ / M11 streaming ✅ ran as normal feature branches). M0–M6 + M8 + M9 all done; **M7 (AWS migration) DROPPED** (user decision 2026-07-07, cut for time — the PaaS runbook in DEPLOY.md is the deployment story; the deploy itself stays manual + user-executed per ADR-009, runbook `ignore/TODO_Railway_deployment.md`, and may still be unperformed). README carries the "Status: complete" cut-lines note (no AWS, no approvals-UI auth, no MCP tracing, static MCP token map). **M6 (observability, ADR-042…045) was the final milestone — on branch `m6-observability`, PR pending.** Post-merge nightly dry-run (2026-07-08): 17/18 e2e, all other suites green — the memory recall probe collided with the knowledge agent's ADR-017 grounding contract; fixed in the PROBE per ADR-032's update, floor untouched.

- **M11 done (ADR-048), branch `m11-streaming`, PR pending:** `POST /chat/stream` (SSE) cuts PERCEIVED latency; the `/chat` JSON contract is FROZEN and stayed byte-identical (its 18 e2e flows ran untouched datasets, all green). The SDK facts were verified live BEFORE the endpoint existed (`ignore/tem/m11_run_streamed_probe.py` — a bad answer would have gated the design): `Runner.run_streamed` persists to the Postgres session store exactly like `Runner.run` (9=9 items, follow-up input tokens prove the history loads; same `save_result_to_session` code inside the SDK) and the ADR-042 bridge traces streamed runs with shape-identical span trees. routes_chat's invariant-dense steps were EXTRACTED into shared helpers (`_inject_user_facts`/`_serve_cache_hit`/`_run_context`/`_flagged_response`/`_finalize_run`) so the two endpoints are one pipeline by construction. Event schema (ADR-048): `delta` (output-text tokens ONLY — reasoning/function-arg deltas dropped server-side) / `status` (handoff+tool run-item events; `transfer_to_*` tool frames suppressed — the SDK models a handoff as a tool call FIRST) / `final` (built from `ChatResponse.model_dump()` — contract drift impossible) / `error` (IN-BAND: the 200 commits when the stream opens; a stream ending without `final` is a failure, EOF is never success). Cache HIT = the stored answer as ONE whole delta + final (~0.2 s, no fake token-drip). ADR-031 under streaming: FastAPI's `BackgroundTasks` does NOT fire under `StreamingResponse` — extraction rides the response's own `background=` task (runs after the stream closes; ordering pinned by test). UI: `st.write_stream` + status captions during the pre-token seconds; transcript truth = the `final` event (streamed text is a live preview; append+rerun architecture unchanged); fallback discipline: an `outcome["started"]` flag separates "stream never began" (→ plain POST /chat) from "died mid-run" (→ error copy, NEVER re-submit — the server-side run may have placed an order). **Gates: tests 173** (+5 `tests/test_streaming.py` off a faked streamed run, +1 route pin), **e2e 19/19** (new `stream_parity` flow: deltas before final, deltas concat == answer, and /chat serves the STREAM-stored cache entry `cached=true` with identical answer+citations — write-gate parity proven live), **slack 5/5**; UI driven end-to-end via `streamlit.testing.v1.AppTest` against a live server (stream + cached repeat + dead-API error copy). **TTFT measured** (M10's 6 probes verbatim, 3×, `ignore/tem/m11_ttft_probe.py`/`m11_ttft.json`): first `status` frame ~1.0–1.2 s everywhere; first TOKEN refusal 8.8 / knowledge 9.2 / order 5.3 / incident-link 9.3 / multi-intent 22.3 s — tokens only exist once the final composition call emits visible text (router handoff, tool turns and the reasoning prefix are structurally token-free; knowledge runs at `medium` per the ADR-047 carve-out), stated honestly in README; stream wall ≈ /chat wall on every probe (streaming moves NO work). **Trace honesty verified live**: trace latency == measured stream wall ±0.02 s, cache-hit traces tagged `cache_hit:true`. **Disclosed, not fixed (pre-existing, probe-surfaced):** a multi-intent run that files a ticket THEN answers ends on knowledge → ADR-023's write gate caches it → an EXACT-text repeat serves the answer and silently drops the new report (probe rep 3 observed cached); candidate fix = exclude runs containing write-tool calls from `is_cacheable`, deliberately out of M11 scope (requirement froze cache logic). Architecture diagram HTML+PNG refreshed (SSE edge + endpoint list). `ignore/LEARNING_M11.md`.
- **M10 done (ADR-047), merged via PR #13:** latency measured then cut, no architecture change. Span-attributed baseline (6 probes × 3 via POST /chat against local Langfuse, `ignore/tem/m10_latency_{baseline,after,final}.py/.json`): **97–99% of cache-miss wall time is LLM generation** (tools 0.02–0.7 s total, handoffs ~1 ms); the tool-less router's single classification call was 6–18 s/conversation and a refusal billed ~1,800 output tokens for ~60 visible — the rest reasoning tokens at the silent gpt-5 default effort ("medium"). Fix = THREE config settings wired into `ModelSettings(reasoning=…)`: `router_reasoning_effort="minimal"` (0 reasoning tokens, routing 30/30 ×3, router gen → 1–3 s), `specialist_reasoning_effort="low"` (fulfillment+incident — all gates green), **`knowledge_reasoning_effort="medium"` — knowledge CARVED OUT after the merge-gate e2e caught a real regression**: at low, the facts-injected chat path decorated 5/13 stage-2 refusals with the forbidden "Sources:" list (ADR-017 violation that ALSO leaks refusals into the semantic cache via the ADR-023 write-time gate); 0 ever at medium; direct-path probes clean 12/12 at both efforts (`ignore/tem/m10_smartwatch_http_probe.py`). Passthrough PROVEN against the logged /responses payload + billed reasoning-token counts (`m10_reasoning_passthrough.py`; NOTE the model path: bare names go to the SDK's default OpenAI Responses provider, NOT LiteLLM — that only handles `litellm/...` names). Final results: refusal 24.2→12.3 s (−49%), knowledge 19.4→15.0 s (−23%, router saving only), order 33.1→11.0 s (−67%), incident-link 27.3→13.0 s (−52%), multi-intent 64.4→42.5 s; cost −24…−53%; subset gate $0.07/run (was $0.10). Final-config gates: e2e **18/18**, subset PASS, full routing 30/30; full `make eval` also ran (dedup 0.917, quality 4.5/4.7, graph PASS; its slack 2/5 was a STALE ADR-046 assertion — suite asserted uuid in replies, product now quotes TKTnnn — fixed in `evals/suite_slack.py`, re-run 5/5; M5 fix-the-probe precedent). **gpt-5-nano router measured and REJECTED** (routing 0.933 then 0.900 = zero margin vs mini's 1.000/1.000, repeated misroute + ping-pong; no latency win left at minimal); found: costs.py prefix-matcher would misprice `gpt-5-nano` at gpt-5 rates (insertion-order trap, noted in ADR-047). Structural floor stated honestly in README: ≥2 sequential calls ≈ 5–9 s/miss at any effort — streaming is M11. Tests 167 (+2: effort wired from config incl. the knowledge split, defaults pinned off Settings fields); README "Latency" subsection; `ignore/LEARNING_M10.md`.
- **M6 done (ADR-042…045), branch `m6-observability`:**
  - **Langfuse wiring (ADR-042):** `app/observability/tracing.py` = `LangfuseTracingProcessor`, a bridge from the Agents SDK's OWN `TracingProcessor` seam (registered once via `init_tracing()` in `create_app()` + the eval CLI) — every `Runner.run` in the process traced (agents/handoffs/tools/generations/guardrails), no per-callsite instrumentation. Callbacks build local OTel spans; Langfuse's background thread does the network — the request path gains nothing. **Empty keys = verified no-op** (no client, no processor, `langfuse` never even imported — process-level test); CI stays keyless and green. **Cloud recommended, but the `.env` keys turned out to be PLACEHOLDERS (`pk-lf-xxx`, 401 on both regions) — M6 verified against the committed OPTIONAL `docker-compose.langfuse.yml`** (v3 six-container stack, own compose project, headless-init dev keys `pk/sk-lf-agentdesk-local`, only :3000 on host — ClickHouse's default 8123 is the e2e port; `make langfuse-up`, UI login admin@local.dev/langfuse-local). **OWNER TODO: create the real Langfuse Cloud project and put real keys in .env.** Price table MOVED to `app/observability/costs.py` (evals/common.py re-imports it — one table, structurally); prefix-matches dated model snapshots, unknown model ⇒ cost None ("unpriced"), never $0.
  - **Trace schema (ADR-043):** trace-end tags `source/intent/agent/cache_hit/flagged` + metadata tokens/cost/handoff_count, session = `group_id` = chat session_id, user = trace metadata. routes_chat opens ONE `trace("chat")` around the whole handler (Runner.run JOINS an ambient trace — that's the SDK behavior that makes it work), so cache-HIT traces exist with honest latency (first minimal-trace version reported 0.00 s; live-verified 0.08 s/$0/`cache_hit:true`, full runs ~20 s with complete span tree). Memory extractor runs as workflow `memory-extraction`, same session group (excluded from chat latency splits, included in conversation cost). Eval cases mint trace ids BEFORE the run (`evals/common.py::eval_run_config`) and record them in row JSON; retrieval-refusal + routing publish to Langfuse datasets (`agentdesk-{suite}`, sha1 item ids, runs linked to traces) every harness run. `scripts/export_metrics.py` (`make metrics`) = the read-only headline table (Langfuse API + the M3 /cache/stats counters BRIDGED, labeled lifetime); no fourth dashboard service on purpose. MCP server deliberately untraced (agent-less separate process — stated in ADR-042).
  - **Verified live + cross-checked:** span tree in Langfuse = router → guardrail → generation → handoff → knowledge → tool → generations with per-call usage/cost; flagged Slack injection trace tagged `flagged:true`/`agent:injection_screen`. **Cross-check: subset run, all 20 billed cases joined by recorded trace id — harness $0.0974 vs Langfuse $0.0974, worst per-case delta $0.0000005** (agreement by construction: same tokens × same table). Headline numbers (61 chat traces): cache-hit p50 0.07 s vs miss 23.6 s; cost/conversation p50 $0.0076; handoffs overwhelmingly 1-hop. All in README "Observability & metrics".
  - **Caching A/B (ADR-044):** `settings.caches_disabled` (env `CACHES_DISABLED=1`) checked FIRST in all three caches — straight through, NO stats bumps (a chosen OFF is not a miss), no warnings; deliberately distinct from Redis-down degradation. Slice: retrieval + e2e (the only suites that touch the caches; the rest call Runner.run directly). Results (one run/arm, 2026-07-08): knowledge_cache flow 21.7 s ON vs 48.9 s OFF (-56%, THE concentrated semantic-cache win); retrieval answerable p50 0.01 s vs 0.25 s (~25×, embedding cache); blended e2e p50 -18%, Langfuse-measured e2e-window spend -13% — README quotes the blended numbers WITH the concentration caveat, per-flow table via `scripts/ab_caching_report.py` (--langfuse-cost closes the ADR-034 e2e cost gap by trace window). OFF arm failed knowledge_cache's `cached=true` assert = the flag working. **Honest flags in README: multi_intent failed BOTH arms** ("incident half acted" — 18/18 at M5 baseline; watch next nightlies), OFF arm additionally dropped incident_create + refusal_to_ticket (non-cache, high-API-latency day, but 3-in-one-run is above historical rate).
  - **Cost budget (ADR-045):** `settings.cost_alert_threshold_usd` (default $0.10, 0 disables) — per-SESSION spend accumulates in Redis (`cost:session:{id}`, INCRBYFLOAT, 7-day TTL, the M3 stats pattern); crossing ⇒ loud `COST BUDGET EXCEEDED` log + `cost_budget_exceeded` WARNING event on the crossing trace; Redis down ⇒ silent per-trace fallback. Alert-only by design (spend authority lives in the ADR-020 HITL gate). LLM-free tested.
  - **Tests 161 pass** (+17 in `tests/test_observability.py`: no-op contract, register-once, tag/cost/session aggregation off fake spans + fake Langfuse, guardrail⇒flagged, cache-hit trace, orphan spans never raise, budget accumulate/fire/degrade/disable, all three caches-disabled seams, eval trace-id hook, **route-table pin** — an M6 helper inserted between `@router.post` and `async def chat` became the handler and 422'd every request, caught live because no test pinned route→endpoint; now one does). FakeRedis gained incrbyfloat/expire. Ruff clean.
  - Scope note: the ONLY agent-path files touched were routes_chat (trace wrapper), extraction.py (workflow tag), the three cache modules (ADR-44 seam), config, main. No agent/tool/instruction changes — eval floors untouched everywhere.

- **Deploy-readiness follow-up (PR #9, merged):** the Docker image — the deploy artifact — had been BROKEN since M9: `app/tools/graph_tools.py` imports the top-level `graph` package, but the wheel packaged only `app` and the Dockerfile never copied it (local venv runs never hit it). Fixed: wheel packages = app + graph + mcp_server, Dockerfile COPYs both; `MCP_HOST` setting (FastMCP host was hardcoded to loopback — undeployable); DEPLOY.md refreshed (M8-services section, migrations 0002/0003 auto-applied note, M5 sessions-survive-redeploys correction, M7 references removed). Verified the real way: image built, imports checked INSIDE it, container booted against compose postgres/redis — migrations ran, /readyz ready, live /chat answered with citations. Step-by-step Railway + Slack-connect runbooks for the user: `ignore/TODO_Railway_deployment.md`, `ignore/TODO_Slack_connect.md`.

- **M8 done (ADR-038…041), merged via PR #8:**
  - **Slack ingest (ADR-038):** Socket Mode over Events API (no public URL while the deploy story is in flux — ADR-009/029; the swap seam is deliberate: only `main()` is Socket-Mode-shaped, an Events-API route would call the same `parse_trigger`/`handle_trigger`). `app/slack/runner.py` (`make slack`) is a PURE HTTP client of POST /chat — never imports an agent, never touches the DB, so "the normal pipeline" holds by construction. Triggers: @mention or :ticket: reaction; acting user = the TRIGGERING user; session_id = `slack:{channel}:{thread_root}` (deterministic — re-triggers continue the conversation). routes_chat gained `source`/`slack_*` request fields and gates the chat-only conveniences: NO semantic-cache serve for slack (a report paraphrasing a KB question must become a ticket, not a cached answer), NO memory hooks (multi-author thread text would poison user_facts).
  - **Identity (ADR-039), fail-closed both surfaces:** Slack profile email must equal a seeded `users.email` via `GET /identity/resolve` BEFORE anything runs (no match → canned runner-side apology, pipeline never invoked); MCP bearer token → email via `MCP_TOKENS="token=email,…"` (full multi-user auth explicitly out of scope). Both feed `ChatContext.user_id` exactly as routes_chat does — `resolve_acting_user` stays the single trust point. Slack thread coordinates travel in ChatContext (trusted, never LLM args): `post_slack_message` (incident agent only, new tool in `app/tools/slack_tools.py`) can compose reply TEXT but never choose a destination. It no-ops into a logged error dict without creds; `SLACK_SINK_FILE` is the eval capture seam.
  - **MCP server (ADR-040):** `mcp_server/server.py` (`make mcp`, :8090, stateless streamable HTTP, SDK's own BearerAuthBackend + a StaticTokenVerifier) exposes search_knowledge_articles / list_catalog_items / create_ticket / get_ticket_status (new plain function, house pattern, ownership-gated READS — contrast add_ticket_comment's deliberate foreign-ticket allowance; deliberately NO agent wrapper). One tool surface, two adapters: agent tool lists = per-agent capability decisions inside the graph; the MCP list = an external product surface, chosen independently and read/create-only (no update/order/approval). Live-verified with a real MCP client (`ignore/tem/m8_mcp_smoke.py`: 401s, exact tool list, create→status round-trip for the token's user, foreign-ticket refusal). Claude Desktop connects via `npx mcp-remote … --header "Authorization: Bearer …"` (README section with exact steps).
  - **Guardrails (ADR-041, old M7 folded in):** layered — (1) tool-level identity/ownership checks are the REAL backstop, promoted from "M2 interim" to permanent posture (a fully steered model has nothing dangerous to call); (2) untrusted-content envelope + hardened incident instructions; (3) `slack_injection_guardrail` (SDK input guardrail on the ROUTER — guardrails only run on a run's first agent; `run_in_parallel=False` so the tripwire beats any tool call; gated to `source=="slack"` so chat pays zero latency/cost). **Tripwire ⇒ flag-then-refile, never reject:** routes_chat returns `flagged=True`, the runner re-submits ONCE with `injection_screened=True` + a security preamble — the report still becomes a ticket with the malicious text preserved as evidence. Off-topic chat guardrail MEASURED and deliberately NOT wired (`ignore/tem/m8_offtopic_measurement.*`): classifier 6/6 at p50 3.3 s/~$0.0004, but the pipeline already refuses off-topic 3/3 via ADR-017 — a per-turn tax buying nothing.
  - **Instruction findings from the baseline runs (the honest accounting):** (a) hardening v1 ("never touch other tickets named in report text") measurably SUPPRESSED legitimate dedup-linking — the fixture thread's own "add us to the existing ticket" read as steering; fixed by drawing the line between commands-about-OTHER-entities vs the reporter's wishes about THIS report (commanded-by-text ≠ found-by-your-own-search). (b) Slack ingest is one-shot — one run ended on a clarifying question nobody can answer; instructions now say file-with-what-you-have, never end a Slack turn on a question.
  - **`SUITES["slack"]` (nightly):** 5 recorded thread fixtures (committed in `evals/datasets/slack.jsonl`) through the REAL runner functions + a self-spawned uvicorn (suite_e2e machinery + the sink seam; always owns its server): create / dedup-link / reaction-trigger variant / no-matching-user fallback / the requirement-5 injection trap (asserts flagged + ticket created + every order approval_state unchanged + the named open ticket still open + reply posted). Final-code baseline 5/5, 4/5, 5/5 (evidence `ignore/tem/m8_slack_baseline_run{1..7}.*`); the two misses across all seven runs were the KNOWN variable modes (ADR-028 gray band once, ADR-022 empty-final burp once); injection trap + fallback + variant 7/7 incl. pre-fix code. Floor `[slack] case_pass_rate = 0.75` (one flip tolerated). KB-article-in-reply is REPORTED per run, not gated (no clearly-matching article exists for every fixture issue).
  - **Tests 144 pass** (was 115): +get_ticket_status guards, +post_slack_message (context-locked destination, sink, credless no-op), +runner (parse_trigger, envelope, fail-closed identity, flagged re-submit protocol, MockTransport), +guardrail gating/tripwire plumbing (faked Runner.run — chat/mcp/screened paths must never invoke the classifier), +MCP (tool-surface pin, token map, identity threading minus HTTP). test_agents pins updated deliberately: incident += search_knowledge_articles (the ONE suggested article in Slack replies, instruction-scoped) + post_slack_message; new pin: the guardrail lives on the router only, sequential. e2e 18/18 re-verified after the routes_chat changes. ChatRequest.message limit 4000→8000 (thread envelopes; runner truncates threads to 6000 keeping the root).

- **M9 done (ADR-035…037), on branch `m9-graph-rag`:**
  - **CMDB schema (ADR-035):** migration `0003` adds `cis` (one generalized node table: service/server/database + TEAMS-AS-NODES, `name` unique = the public identity) + `dependencies` (directed edges, "dependent DEPENDS ON dependency", `runs_on/uses/calls` as display metadata, both columns indexed, self-loops CHECKed out — cycles are the traversal's job). `assets` deliberately untouched (user devices ≠ shared infrastructure). Applies + downgrades cleanly (verified both ways).
  - **Data (merge-append, zero churn):** taxonomy gained a hand-authored `infrastructure` section → 59 CIs / 82 edges (auth-service = the shared node with 6 callers; shared DBs crm-db/hr-db; shared hosts db-server-01/06/07; monitoring-* deliberately isolated as the blast-radius control), 7 runbook articles carrying every ONE-HOP fact (deterministic templates — they are eval ground truth, LLM prose could garble it), 6 CI tickets (topics chosen away from the dedup groups). `git diff --numstat data/` shows only additions; regeneration is idempotent. GOTCHA: plain `--stage 2` re-pays LLM prose for all 200 articles before the write-skip — use `--dry-run` for M9 regeneration (all new prose is templated). `make seed` stays the fresh-volume path (its chunk merge collides with ingest's re-chunked ids on a live DB — pre-existing; locally: insert rows + `make ingest`).
  - **Graph tool (ADR-035/004):** `app/tools/graph_tools.py::query_dependency_graph(entity, direction, max_depth)` — name→CI resolution with candidate suggestions (never auto-pick), enum/depth guards, teams→user-count rollup, `guidance` in the payload, no bare "results" key. Wired into the INCIDENT agent (impact/blast-radius/shared-root-cause is its domain; instructions draw the similarity-vs-graph line explicitly); knowledge agent untouched (ADR-017 contract stays pristine). Tool pins updated deliberately in tests/test_agents.py.
  - **Two backends, one contract (ADR-037):** `graph/postgres_graph.py` (recursive CTE, path-array cycle guard, GROUP BY/MIN shortest-depth) is the DEFAULT; `graph/neo4j_graph.py` (Cypher, lazy driver, loud failure, NO silent fallback) behind `settings.graph_backend`; `python -m graph.sync_neo4j` = idempotent convergent projection (MERGE + delete-stale; Postgres stays source of truth). Compose gained an OPTIONAL `neo4j` service nothing depends on. **Parity finding:** Cypher's relationship isomorphism re-emits the START node when a cycle closes back on it (CTE's path guard never does) — caught by the synthetic-cycle test, fixed with `WHERE n <> start`. 16 tests in tests/test_graph_tools.py (exact seeded sets incl. the crm-service trap + monitoring control, depth/direction edges, cycle + diamond semantics, tool payload contract, all-CI both-direction parity); neo4j tests self-skip when the server is absent — CI stays green with no Neo4j (115 tests pass locally with Neo4j up; the 7 neo4j-gated ones skip elsewhere).
  - **The flagship comparison (ADR-036):** `SUITES["graph"]`, nightly — 15 multi-hop cases (`graph.jsonl`, committed ground truth computed from the seeded graph), three arms (knowledge agent = plain RAG / incident agent + CTE / same + Neo4j via the switch, self-skips where absent), scored by closed-universe CI-name extraction → set P/R/F1 (citation titles stripped — runbook titles contain CI names, a scoring-fairness bug caught between baseline runs). **Three-run baseline: plain RAG F1 0.44/0.64/0.47 with 3–5 refusals per run (the ADR-017 contract treats chained-runbook evidence as a coin flip — the HEADLINE is RAG's variance, and db-server-01's 18-CI/4-hop case scored 0.20 every run); both graph arms F1 1.000, 45/45 exact sets; tool-level latency (LLM-free, 20 reps) postgres ≈ neo4j ≈ 2–3 ms p50 — no latency story at 59 nodes.** Floor: `[graph] cte_f1_mean = 0.8` (below a 1.000×3 baseline; rag deliberately floor-free — it's the comparison baseline; neo4j arm gates via the hard-coded parity check). Suite ≈ $0.15 and ~9–10 min/run local (~6 min in CI) — nightly timeout 90→120 min from measurement. Evidence: `ignore/tem/m9_graph_baseline_run{1,2,3}.{json,log}`.
  - **Fairness re-verification after adding runbooks:** retrieval suite unchanged (recall@5 1.000 / MRR 0.983 / false refusals 0; agent-mode refusals 10/10); dedup suite re-run green at 11/12, inside the historical 8–12 range (CI-ticket topics chosen away from the seeded groups). README gained the flagship "Plain RAG vs Graph-RAG" section + results table; ERD (re-rendered via headless Chrome from `database_erd.html`) + DATA_DICTIONARY current.

- **M5 done (ADR-030…034), on branch `m5-memory-evals`:**
  - **Sessions → Postgres (ADR-030):** the SDK's own `SQLAlchemySession` (0.17.7) behind the designed ADR-019 swap point — ONLY the body of `routes_chat._load_session` changed. Same `postgresql+psycopg` URL drives the async engine (no asyncpg); Alembic migration `0002` mirrors the SDK's `agent_sessions`/`agent_messages` schema exactly and the store runs `create_tables=False` (Alembic stays the single schema owner). The `ignore/chat_sessions.sqlite3` stopgap is DELETED. Restart survival proven twice: unit (fresh store instance) and e2e `chat_restart` (kill + respawn the server mid-session, turn-1 token recalled).
  - **Long-term memory (ADR-031):** inject = ONE `role:system` item added to the session on a conversation's first turn (persists with the conversation, every handed-off agent sees it, ≥0.5 confidence only); extract = one gpt-5-mini structured-output call per turn via FastAPI `BackgroundTasks` (post-response — can never delay or 500 a reply; "session end" doesn't exist in an HTTP chat API). Dedup/merge is deterministic and LLM-free (`user_facts.apply_extracted_facts`): same fact_type + same text skips, contradictions REPLACE at ≥ existing confidence, hesitation never overwrites belief. Facts are plain user-scoped functions (ADR-004 discipline), NOT agent tools. Live-loop proven by `ignore/tem/m5_memory_smoke.py` before the e2e flow formalized it (one empty-answer scare = the known ~1/30 ADR-022 burp, reproduced 0/3).
  - **tests/test_memory.py:** 11 LLM-free tests (store round-trip/isolation/restart, ADR-023 first-turn policy against the new store, injection formatting + confidence gate, the full merge rule, extraction plumbing with a faked `Runner.run`). 99 tests total pass.
  - **Quality suite (ADR-033):** `SUITES["quality"]`, nightly-only — 10 knowledge answers scored 1–5 on faithfulness-to-citations (judged against the retrieved CHUNK TEXTS from the run's tool outputs) and helpfulness, by `settings.judge_model` = **gpt-5** (stronger sibling — self-preference bias; verified against the live account model list). Judge rubric committed verbatim in `evals/judge_prompt.md`; structured integer output. REPORT-ONLY, no floor: faithfulness mean moved 4.7→4.1 across two same-day runs on identical code — that variance is why (ADR-026's own rule).
  - **E2E 6→18 flows (ADR-032)** on the existing machinery: ≤$500 auto-place + form_values-vs-form_schema, windows-only AutoCAD → NO order row (negative side effect), update_ticket on the user's own seeded row (restored after), second link/create dedup pair (SSO + desk phone), identity isolation as a second user (avery), knowledge→incident refusal-ticket edge, multi-intent, memory carryover (fact extracted in session A used in session B), chat restart. Dataset ORDER is load-bearing (constraints in the jsonl notes). Cleanup now restores user_facts CONTENT (extractor may update seeded rows in place) and deletes session rows; server shutdown escalates to SIGKILL so `_cleanup` can never be skipped (a graceful-shutdown timeout was observed skipping it). First 18-flow run: 16/18 — both failures were harness-design bugs (stage-2 refusals legitimately carry adjacent-article citations, so zero-citations is now dataset-scoped via `stage1`; ticket_update needed the ADR-027-addendum bounded nudge), fixed in the flows, floor untouched at 1.0.
  - **Retrieval 30→40:** +5 answerable (all recall 1.0, gate-clearing — verified LLM-free before commit) and refusal slice 5→10 (a real RATE now; observed 8–10/10 across three same-day runs — misses are gray-band probes doing ADR-017 stage-2 judgment). Floors unchanged.
  - **Wrong-handoff matrix:** routing report now carries a full expected×actual confusion matrix (JSON + pretty table + CI summary) — accuracy says how often routing fails, the matrix says WHERE it leaks.
  - **Per-case cost + latency (ADR-034):** wall-clock on every row; tokens + `cost_usd` from SDK run usage × the committed price table (`evals/common.py`, checked 2026-07-07); suite totals + p50/p95 everywhere; `--out` writes full JSON. **`evals/results/baseline.json` is a committed full run.** e2e rows are latency-only (tokens bill inside the spawned server — the M6 Langfuse cross-check closes that gap). Honest re-measurement: the subset gate costs ≈**$0.10**/run, not the never-metered "$0.02–0.05" — the harness prints the measured cost each run. Nightly timeout 60→90 min (measured 37-min local full run + observed 2× CI latency).
  - **M4's "ticket_comment leak" re-diagnosed and fixed:** it was never killed-process windows — `test_comment_on_foreign_ticket_is_allowed_for_dedup` writes onto a SEEDED ticket, which the `clean_writes` cascade never removes, so every `pytest tests/` run leaked one "license expired" comment (confirmed by timestamp: one appeared during a test pass with no eval running). The fixture now diffs comments explicitly; the 9 accumulated leaks were deleted by explicit id (the intentional M2 demo "me too" printer comment kept). Related: e2e cleanup now also restores seeded-ticket status/priority content — the first baseline run's multi_intent agent bumped the Wi-Fi ticket to high while linking, invisible to id-diffs (ADR-032 update).

- **M4 done (ADR-026…029), on branch `m4-ci-evals` pending PR review/merge:**
  - **Floors → `evals/thresholds.toml`** (ADR-026, single source of truth — the harness reads it, CI runs the harness): retrieval recall@5 ≥ 0.8 / refusals ≥ 0.8 / false refusals ≤ 0; routing ≥ 0.9, integrity ≤ 1; e2e all-flows; dedup ≥ 0.65. Floors are regression gates below OBSERVED variance (concretizes ADR-010) and bind to gpt-5-mini + text-embedding-3-small (never swap models for CI). Three were recalibrated 2026-07-07 after the first full-run variance data (nightlies + PR #4 CI): integrity 0→1 (the ~1/30 ADR-022 burp), dedup 0.75→0.65 (observed 8–12/12 on identical code), refusals 1.0→0.8 (the whiteboard probe sits 0.002 under the 0.45 gate; its refusal hinges on LLM query expansion — observed flipping in CI; ADR-026 addendum).
  - **`--subset` / `make eval-subset`** (the PR gate): full retrieval (answerable slice is LLM-free; 5 refusal agent calls) + 10 routing cases flagged `"subset": true` in routing.jsonl (all 6 hard + 1 easy per specialist + update path). Deterministic by construction; ≈$0.02–0.05/run. Verified 10/10 + retrieval unchanged.
  - **`SUITES["e2e"]`** (ADR-027, `evals/suite_e2e.py` + `datasets/e2e.jsonl`, nightly): 6 flows through a self-spawned uvicorn on :8123 (refuses stale servers; `E2E_API_URL` overrides), asserting SIDE EFFECTS for the RIGHT user: order approve ($840 Tableau → pending → fresh-client approve → placed) and reject ($999 iPhone → cancelled; AutoCAD was measured failing correctly — windows-only vs the demo mac), incident link (comment on a seeded open printer ticket, no new ticket; report phrasing pre-authorizes the action) and create (embedded, right owner), knowledge (citations + fresh-session paraphrase served cached=true — the M3 contract asserted as product), refusal (zero citations, never stored). semcache flushed in setup; snapshot-delete cleanup. Verified 6/6. Wall time is LLM-latency-bound: 5–35 min measured for the same flows.
  - **`SUITES["dedup"]`** (ADR-028, `evals/suite_dedup.py` + `datasets/dedup.jsonl`, nightly): the ADR-021 gray band finally measured — 6 shared-infra link probes + 6 traps through the incident agent with real tools, scored on the DB action, per-case cleanup. All probes in-band (top sim 0.52–0.76). **Baseline 12/12; two probes observed flipping across runs → floor 0.75.** Design findings: per-device issues (battery/docking) must NOT be link probes (another user's device = different asset — agent creating there is correct); one outage can span two seeded groups (DNS probe accepts either). This suite is the evidence base for the deferred cross-encoder decision.
  - **ci.yml** (every PR + push main): ruff check + format --check (repo ruff-formatted in M4 — one mechanical commit); pytest vs pgvector/redis service containers mirroring compose exactly, after migrate→seed→ingest (~$0.01 cold-cache embeds/run); **REQUIRE_DB=1** (conftest raises at collection if Postgres unreachable — kills the green-but-empty silent-skip trap); eval subset; metrics table auto-appended to $GITHUB_STEP_SUMMARY by the harness; missing OPENAI_API_KEY fails a loud guard step (fork PRs get no secrets by design); pip cached, concurrency-cancel per ref.
  - **nightly.yml** (18:00 UTC + workflow_dispatch): all four suites, full metrics in the job summary; failures = GitHub's default email. 60-min timeout (e2e latency variance).
  - **deploy.yml INERT** (ADR-029, reconciled with ADR-009 — first deploy still manual, not done): dispatch always runs but fails loudly at a secrets guard; push-to-main gated on repo var `DEPLOY_ENABLED == 'true'`. Armed flow: docker build → railway up → explicit `alembic upgrade head` release step → poll **/readyz** 5 min. actionlint-clean; arming runbook in DEPLOY.md ("CI deploy"); live verification deferred until armed.
  - Tests 88 pass (conftest gained only the REQUIRE_DB guard), ruff + format clean. One local ticket_comment leak observed during back-to-back eval runs (deleted; suspected killed-process window, CI immune — ephemeral DB). Writeup: `ignore/LEARNING_M4.md`.
  - **CI proven the honest way (PRs #2/#3, closed after proof):** inverted ownership guard → CI red at the **Tests** step on exactly the 2 DB-backed ownership tests (also proving service-container DB tests really run under REQUIRE_DB=1); broken RRF fusion (FULL OUTER JOIN→JOIN) → **all 88 unit tests green, eval-subset gate red** (recall@5 0.700 vs floor 0.8, 8 false refusals — CI numbers matched local exactly). Both reverts → green. Measured CI wall time **8m11s** (install 36s / seed+ingest 18s / pytest 12s / eval subset 6m44s — LLM-latency-bound; routing cases stay sequential on purpose so cases can't see each other's tickets in dedup searches).
  - **Nightly dry-run (post-merge, 2026-07-07):** first dispatch was **red — and correctly read, that was the system working**: retrieval PASS, **e2e 6/6 PASS in CI**, routing accuracy 1.000 but 1/30 integrity burp (the residual ADR-022 empty-final mode), dedup **8/12** (below all local runs; one trap linked at 0.495 — below the gray band — and two probes took NO action, the "lost report" mode). Floors recalibrated per ADR-026's own below-observed-variance rule (integrity ≤ 1, dedup ≥ 0.65 — ADR-028 addendum documents it). The second dispatch flipped the failures: routing 30/30 integrity 0, dedup 12/12 — but e2e's order_approve died on conversation SHAPE (agent asked an extra question, the 2-turn script ran out). Fixed in the flow, not the floor: order flows now nudge like a real user (≤2 bounded turns until submitted/pending exists, nudge count reported — ADR-027 addendum); the side-effect assertions are untouched. Verified by branch re-dispatch before merging. The deploy workflow's inert gate also proved itself live: the merge push showed Deploy as `skipped`.

- **M3 done (all acceptance criteria met; ADR-023…025):**
  - **Semantic cache (ADR-023)**: `app/cache/semantic_cache.py` + pre-check in `routes_chat` BEFORE `Runner.run`. Read-only guarantee enforced at WRITE time — only runs ending on `knowledge` with the ADR-017 contract ("Sources:" + citations) are stored, so refusals/actions/errors are never cacheable and no read-time classifier exists. Session policy: consult+store on a conversation's FIRST turn only; on a hit, inject the Q/A into the session (`add_items`) so follow-ups stay coherent. Entries GLOBAL across users (knowledge answers are user-independent; revisit if a user-scoped tool lands on knowledge). Brute-force cosine in app code (stock redis:7-alpine has no vector module; revisit at ~thousands of entries). TTL 24 h.
  - **Threshold 0.75, measured NOT 0.95** (`ignore/tem/m3_semantic_cache_demo.py`, 18 probes): tight paraphrases 0.816–0.937, near-misses ≤0.672 ("change my wifi password"), LOOSE paraphrases OVERLAP the near-miss band (0.641–0.826) — the ADR-017/021 lesson a third time; no threshold serves them safely. 0.75 = midpoint of the separable gap, biased toward false misses (safe: agent re-runs) over false hits (wrong answer served). At the suggested 0.95 even "how can I reset my password?" (0.937) would miss — the cache would never fire. Live: 5/5 paraphrase hits at 67–75 ms vs 14–67 s uncached, 0/8 near-miss false hits; fulfillment query demonstrably never served/stored.
  - **Invalidation (ADR-024)**: per-article CONTENT HASH diff inside `ingest_articles` (the single write path) — old chunk content from the DB vs freshly chunked output, then delete entries whose `cited_article_ids` intersect. NOT `updated_at` (SQLAlchemy client-side `onupdate`; raw-SQL edits never bump it). Chunker change ⇒ everything invalidates (correct). Verified live: edit → ingest → exactly the 1 citing entry deleted, 2 kept.
  - **Response cache (ADR-025)**: `@cache_response(key_fn, ttl=300s)` on the PLAIN `list_catalog_items` (key: os_filter) and `get_user_assets` (key: trusted `ctx.context.user_id` — never an LLM arg) before `function_tool` wrapping (verified SDK 0.17.7 derives schema through `functools.wraps`). Error dicts never stored (SDK self-correction needs fresh evals). `get_user_profile` deliberately uncached (feeds order forms). No write-side invalidation — nothing in-app mutates assets/catalog yet; trigger documented.
  - **Counters + `GET /cache/stats`**: `app/cache/stats.py`, persistent Redis INCRs (`cache:stats:{name}:{hit|miss}`) shared by all three caches; endpoint in `routes_health`. Counter writes never fail a request. This is the M6→Langfuse surface.
  - `ChatResponse` gained `cached: bool = false`; Streamlit shows a "served from cache" caption. Cache hits return STORED citations (never `_collect_citations`). All caches degrade silently when Redis is down (DownRedis-tested).
  - Tests: 88 pass (65 pre-M3 + 23 new in `tests/test_cache.py`; FakeRedis extended with get/setex/delete/incrby/ttl/scan_iter + autouse fixture isolating the response cache — decorated tools run in every tool test). `make eval` unchanged: retrieval recall@5 1.000/MRR 0.980/refusals 5/5; routing accuracy 1.000/ping-pong 0/integrity 0 (evals call `Runner.run` directly, bypassing routes_chat by construction). Evidence: `ignore/tem/m3_semantic_cache_demo.py` (+ saved output `m3_demo_output.txt`); writeup `ignore/LEARNING_M3.md`.

- **M2 done (all acceptance criteria met):**
  - **Tools first** (`app/tools/{user,ticket,catalog}_tools.py`): identity from `ChatContext` via `RunContextWrapper` (never an LLM arg), parse→exists→ownership for user-referenced ids, StrEnum-typed args + belt-and-suspenders `enum_error` for direct callers, prices always read from the DB row. `user_tools.py` owns the shared guards (`resolve_acting_user`, `parse_uuid_arg`, `enum_error`). Action payloads must never use a bare `"results"` key (the `_collect_citations` extractor would leak tickets/orders into citations). `tests/test_tools.py`: 28 tests incl. every guard.
  - **Order/HITL state machine (ADR-020)**: `place_catalog_order` self-gates on DB price — ≤$500 places (`submitted`/`not_required`), >$500 saves a `draft`; `request_approval` flips draft→`submitted`+`pending` (the one legal transition under the M0 CHECK) and the run ENDS. `/approvals` API + `ui/approval_view.py` (`make approvals`, :8502) approve (→placed) / reject (→`rejected`+`cancelled`) from a fresh process. `approve_order`/`reject_order` are plain functions, deliberately NOT agent tools. Native SDK `needs_approval`/RunState was evaluated live and rejected (evidence in ADR-020; experiment in `ignore/tem/m2_throwaway_react.py`).
  - **Session stopgap (ADR-019)**: `ChatRequest.session_id` → file-backed `SQLiteSession` under `ignore/`; the single M5 swap point is `routes_chat._load_session`. Streamlit mints a session_id per browser session and has an acting-user picker (default `demo.user@corp.com`). Every turn re-enters the router (it re-classifies with replayed history).
  - **Graph (ADR-022)**: router→{knowledge, fulfillment, incident}; knowledge→incident (user accepted the refusal ticket offer); specialist→router back-edges ONLY on domain change. All cross-agent edges are wired post-construction in `router.py` (the assembly point — import cycles otherwise); `tests/test_agents.py` pins the exact edge set, tool sets, and guards. **Key finding: the SDK counts a HANDOFF as tool use for `reset_tool_choice`**, so the router's first handoff flipped it to `"auto"` and a back-edge re-entry produced an empty non-answer (ADR-018 one layer up, caught by the routing eval) — the router now sets `reset_tool_choice=False` (it never speaks); specialists keep the default True.
  - **Incident dedup (ADR-021, measured)**: embeddings CANNOT separate "fresh report of the same issue" (0.59–0.77 vs its group) from "similar but different" (cross-issue NN p95 0.754) — so dedup is a two-stage cascade like ADR-017: deterministic `likely_duplicate` flag at cosine ≥0.80 in `search_similar_tickets` (0% cross-issue FPs on seed) + agent judgment in the gray band, with `guidance` in the tool payload ("flag not set ≠ not a duplicate" — without that line the agent treated the flag as authority and created a duplicate; observed live, fixed, re-verified). Agent searches with its FORMAL DRAFT (`"title\n\ndescription"`, the ingest format), never raw user text (register mismatch, measured). Sweep evidence: `ignore/tem/m2_dedup_sweep.py`.
  - **Routing eval (moved M4→M2)**: `SUITES["routing"]`, 30 cases (`evals/datasets/routing.jsonl`: 8/8/8 + 6 hard) run THROUGH the router with real tools (~30 LLM runs; created rows are snapshot-deleted). Final: **accuracy 1.000 (hard 6/6), ping-pong mean/max 0.00/0, integrity failures 0, multi-intent fires both knowledge tools**. The suite caught two real bugs pre-fix (router reset issue above; fulfillment back-edging on "item not found"). Retrieval suite unchanged: recall@5 1.000, MRR 0.980, refusals 5/5. `--suite retrieval` stays the cheap loop.
  - **E2E verified against the live stack** (`ignore/tem/m2_e2e_acceptance.py`, exact HTTP contract the UIs speak): Tableau $840 order via 2-turn chat → pending, run ends → **API killed and restarted** → approved → placed; seeded $840 Tableau (avery) rejected → cancelled; near-duplicate printer report LINKS (comment on seeded open ticket, no new ticket); fresh webcam issue creates an embedded ticket. Both Streamlit surfaces boot and serve. Live routing fix along the way: license *acquisition* routes to fulfillment even though the KB documents the ordering process.
  - **Demo DB state after acceptance** (intentional): demo user's seeded $650 Photoshop order still pending (for live demos), avery's Tableau rejected/cancelled, one approved Tableau order, one webcam ticket, one "me too" comment on the seeded printer ticket.
  - **Interim safety**: M2 had real side effects with guardrails still milestones away — the tools' own identity/ownership checks were the deliberate interim defense, later promoted to permanent posture when guardrails landed in M8 (ADR-041).
  - **Docker gotcha**: the compose app/ui images are M1-era on disk — always `docker compose up --build` to pick up M2 code (a plain `up` boots stale M1 images). `docker-compose.yml` + `DEPLOY.md` now include the M2 **approvals** service (`ui/approval_view.py`, port 8502) as a third stateless service off the same image. (During e2e a stale `make dev` uvicorn + old compose app/ui were squatting on :8000/:8501 and swallowing requests — stopped them; postgres/redis kept.)
  - 65 tests total pass (30 pre-M2 → 28 tool tests + graph pins); ruff clean. Writeup: `ignore/LEARNING_M2.md`.

- **M1 done (all acceptance criteria met):** hybrid RAG (`app/rag/*`: heading-aware chunker w/ contextual continuation headers, LiteLLM embeddings, single-SQL RRF hybrid search with ADR-013 pre-filters), embedding cache (`app/cache/{redis_client,embedding_cache}.py` — **pulled forward from M3**, so M3 shrinks to semantic + response caches), atomic-rebuild ingest (`make ingest`, idempotent: 2nd run = 440 cache hits / 0 embeds), knowledge tools (only DB path), tool-less router + knowledge agent (Agents SDK, gpt-5-mini), `POST /chat` + `/healthz` + `/readyz`, Streamlit chat with citations, retrieval evals (`make eval`: **recall@5 = 1.000, MRR = 0.980, refusals 5/5, false refusals 0**), Dockerfile + compose (app + ui services) verified e2e, `DEPLOY.md` (Railway/Render; deploy itself is manual, not done yet).
- **Key M1 finding (ADR-017):** near-miss negative space ("email on smartwatch" vs the email-on-phone article) is measurably inseparable at the retrieval level — refusal is a two-stage cascade: deterministic cosine gate (0.45) in the tools + agent-level coverage check bound to an output contract (answers end with "Sources:", refusals never do + offer a ticket). The eval keys on that contract; keep `app/agents/knowledge.py` instructions and `evals/run_evals.py::_agent_refused` in sync. 440 chunks (not 400 — M1 re-chunked M0's data via `make ingest`); ticket embeddings also populated (invariant 3).
- **M1 follow-up fix (ADR-018):** the router→knowledge handoff was flaky — the destination agent sometimes narrated "You're being transferred…" with no tool call and the run ended (a non-answer). Fixed on both agents with the SDK `RECOMMENDED_PROMPT_PREFIX` + `ModelSettings(tool_choice="required")` (relies on `reset_tool_choice` default True). Now deterministic (10/10 direct, 4/4 live). **The evals do NOT exercise the handoff path** (they call `knowledge_agent` directly) — this bug was found by hand; the routing/ping-pong eval suite is still owed in M4 (concrete regression note in `evals/run_evals.py`). When M2 adds fulfillment/incident handoffs, note the router can no longer emit its own text (tool_choice forces a handoff), so any "can't do that yet" copy must live in a specialist.
- **M1 follow-up hardening:** `knowledge_tools.py` `search_knowledge_articles.category` typed `str` → `TicketCategory` enum, so the SDK schema carries `enum:[…]` and strict structured outputs constrain the arg at decode time (shape enforced at generation, not just requested); manual `_VALID_CATEGORIES` check kept as belt-and-suspenders for direct callers. Argument-*semantics* (e.g. an LLM passing a name where an id belongs) can't be schema-fixed — the pattern (inject identity from `ChatContext`; parse→exists→ownership for user-referenced entities) is captured as the M2 DESIGN NOTE in `app/tools/user_tools.py`.
- **M0 done:** `app/db/{database,models}.py` (9 SQLAlchemy models), Alembic migration `0001` (pgvector ext, HNSW + GIN indexes, generated `tsv`, composite-FK invariant, `approval_state` CHECK, `COMMENT ON` docs, `trg_sync_chunk_metadata` trigger), `scripts/generate_data.py` (2-stage, deterministic uuid5, `--dry-run`), `scripts/seed_db.py`. Data seeded: 50 users / 114 assets / 200 articles / 30 catalog / 16 orders / 300 tickets / 45 comments / 3 facts. `evals/datasets/retrieval.jsonl` (25 answerable + 5 refusal).
- **Local dev is fully set up:** `.venv` (Python 3.12) with the complete `-e ".[dev]"` install (2026-07-05: the M0-era `cryptography` Rust-build failure resolved itself once pip was current; installed with `--only-binary cryptography` as a safety net, keep that flag if reinstalling). 30 tests pass (`make test`; DB-backed ones auto-skip if Postgres is down), ruff clean — incl. `tests/test_agents.py`, fast LLM-free assertions pinning the ADR-018 config (tool_choice/prefix) + ADR-003 router-is-tool-less. `.env` exists with a working `OPENAI_API_KEY` (model vars are blank → config defaults: gpt-5-mini + text-embedding-3-small). Docker Postgres+Redis running; DB migrated + seeded + ingested.
- Repo is live at **github.com/lifenyan/agent-desk** (PUBLIC), branch `main`, first 2 commits pushed 2026-07-07 (M0–M2, M3) by the user. `ignore/` + `.env` verified excluded. `gh` CLI installed + authenticated as `lifenyan`; `OPENAI_API_KEY` is set as an Actions repo secret (verified 2026-07-07).
- Renamed from `itsm-ai` → `agentdesk`; `design/` has the diagrams; `ignore/` is git-ignored scratch (holds `LEARNING_M{0..5,8,9}.md`, `M{0..9}_PROMPT.md`, `refusal_threshold_sweep.md`, and `ignore/tem/` with the M9 graph-baseline evidence `m9_graph_baseline_run{1,2,3}.{json,log}`, the M2 evidence scripts `m2_dedup_sweep.py` / `m2_throwaway_react.py` / `m2_graph_smoke.py` / `m2_e2e_acceptance.py`, the M3 evidence `m3_semantic_cache_demo.py` + `m3_demo_output.txt`, the M5 live-loop evidence `m5_memory_smoke.py`, and the M8 evidence `m8_mcp_smoke.py` + `m8_offtopic_measurement.py` (+ saved output) + `m8_slack_baseline_run{1..7}.{json,log}` + `m8_e2e_regression.{json,log}`; the M2/M3 acceptance scripts are formalized as the M4 e2e/dedup suites, the M5 smoke as the e2e memory/restart flows. The ADR-019 sqlite session file was DELETED in M5 — sessions live in Postgres now). `LEARNING_M1.md` §10–14 = Agents-SDK Q&A; `LEARNING_M{2..5}.md` = the per-milestone "what you should understand" writeups.
- `DECISIONS.md` has **48** ADRs (post-completion PRs added: ADR-046 user-facing record numbers ORDnnn/TKTnnn — DB-sequence-assigned, tools resolve both forms, get_ticket_status promoted onto the incident agent; ADR-047 reasoning-effort latency knob — span-attributed measurement, config-driven minimal/low defaults, gpt-5-nano router measured-and-rejected; ADR-048 streaming — SSE /chat/stream as a second presentation of the frozen /chat pipeline, event schema + in-band errors, run_streamed sessions/tracing parity verified live, TTFT anatomy stated honestly, multi-intent cache exposure disclosed. M6 added: ADR-042 Langfuse Cloud + SDK-trace-processor bridge + empty-keys no-op, ADR-043 tag schema/session join/cache-hit traces/eval datasets, ADR-044 CACHES_DISABLED A/B + slice choice, ADR-045 Redis-accumulated per-conversation cost alert. M8 added: ADR-038 Socket Mode + runner-as-pure-HTTP-client, ADR-039 fail-closed external identity + thread coords as trusted context + the sink seam, ADR-040 one plain layer/two adapters + read-create-only MCP surface, ADR-041 layered guardrails — tool checks as the real backstop, flag-then-refile, off-topic screen measured and not wired. M9 added: ADR-035 CMDB modeling — generalized `cis` + `dependencies`, teams-as-nodes; ADR-036 the fair three-way comparison design + measured results; ADR-037 backend contract, Postgres default, when Neo4j earns its cost). Before M9 it had **34** ADRs (M1: ADR-016 chunking/ingest, ADR-017 two-stage refusal cascade, ADR-018 deterministic handoff via tool_choice; M2: ADR-019 session stopgap, ADR-020 hand-rolled HITL, ADR-021 dedup cascade, ADR-022 graph assembly + router never resets tool choice; M3: ADR-023 semantic cache — write-time read-only gate, first-turn session policy, measured 0.75 threshold; ADR-024 content-hash invalidation in ingest; ADR-025 response-cache scope; M4: ADR-026 floors in thresholds.toml + deterministic subset, ADR-027 e2e through the real HTTP contract, ADR-028 dedup gray-band eval + baseline, ADR-029 inert deploy workflow; M5: ADR-030 SDK SQLAlchemySession on Postgres with Alembic-owned schema, ADR-031 fact inject-as-session-item + post-response extraction + deterministic merge, ADR-032 e2e 6→18 with load-bearing dataset order + content-restoring cleanup, ADR-033 gpt-5 judge with committed rubric, ADR-034 cost/latency from SDK usage × committed prices + committed baseline). Data spec: `design/DATA_DICTIONARY.md` + `design/database_erd.png` (source of truth, both current). The two architecture diagrams (`design/agent_handoff_workflow.png`, `design/itsm_ai_system_architecture.png`) were refreshed at M6 closeout — the Jul 3 sketches predated every milestone (triage→router rename, missing ADR-022 cross-edges and the cache short-circuit, Redis wrongly holding sessions, no M8 surfaces, Neo4j as a peer store) — and now have committed HTML sources next to them (ERD pattern: edit HTML, re-render via the headless-Chrome command in each file's header comment).

**To resume locally:** `make db-up` → (if fresh volume) `make seed && make ingest` → `alembic upgrade head` (0002 sessions, 0003 CMDB graph) → `make dev` (API :8000 + UI :8501) and `make approvals` (:8502). M8 surfaces (both optional, everything runs without them): `make mcp` (:8090; needs `MCP_TOKENS=token=email` in .env — README "MCP server" section) and `make slack` (needs the two Slack tokens per SLACK_SETUP.md + the API up). Optional Neo4j (M9 Phase 2): `docker compose up -d neo4j` → `python -m graph.sync_neo4j` → `GRAPH_BACKEND=neo4j`; everything runs without it (postgres backend is the default). M6 observability: `make metrics` prints the headline table (needs LANGFUSE keys); `make langfuse-up` boots the OPTIONAL local Langfuse (UI :3000, dev keys pk/sk-lf-agentdesk-local, login admin@local.dev/langfuse-local); with keys set, every `make eval` run is traced and retrieval/routing publish Langfuse dataset runs; the caching A/B = the two-arm procedure in `scripts/ab_caching_report.py`'s docstring. `make eval` is the FULL nightly-sized run (retrieval 40 + routing 30 + e2e 19 flows + dedup + quality + graph — pre-M9 measured 33–37 min and ≈$0.42; graph adds ≈9–10 min and ≈$0.15; slack adds ≈2.5–3 min, bills inside the spawned server like e2e). Cheap loops: `--suite retrieval` (LLM-light) or `make eval-subset` (the PR gate, ≈$0.07 measured post-M10; was $0.10 at default reasoning effort). `make ci-local` mirrors the CI gates. Full stack in Docker: `docker compose up --build` (the app image was verified current 2026-07-07 post-PR#9; always `--build` after pulling — stale images were an M2-era trap and the M9-era image was silently broken until PR #9).

## Milestones & where the work lives

| # | Milestone | Files |
|---|-----------|-------|
| ~~M0~~ | ~~Data & schema~~ **✅ DONE** | `app/db/models.py`, `app/db/database.py`, `app/db/migrations/0001`, `scripts/generate_data.py`, `scripts/seed_db.py` |
| ~~M1~~ | ~~Core loop: router + knowledge, RAG, chat/health API, UI~~ **✅ DONE** (incl. embedding cache from M3, retrieval slice of evals from M4, `DEPLOY.md`) | `app/agents/{router,knowledge,context}.py`, `app/rag/*`, `app/cache/{redis_client,embedding_cache}.py`, `app/tools/knowledge_tools.py`, `app/api/routes_{chat,health}.py`, `ui/streamlit_app.py`, `evals/{metrics,run_evals}.py` |
| ~~M2~~ | ~~Action agents + HITL~~ **✅ DONE** (incl. routing eval slice of M4, session stopgap from M5; ADR-019…022) | `app/agents/{fulfillment,incident,router}.py`, `app/tools/{user,ticket,catalog}_tools.py`, `app/api/routes_approvals.py`, `ui/approval_view.py`, `evals/datasets/routing.jsonl` |
| ~~M3~~ | ~~Caching~~ **✅ DONE** (semantic cache before any agent, read-only via write-time gating, threshold 0.75 measured; content-hash invalidation in ingest; response-cache decorator; counters + /cache/stats; ADR-023…025) | `app/cache/{semantic_cache,response_cache,stats}.py`, hooks in `app/api/routes_{chat,health}.py`, `app/rag/ingest.py`, `app/tools/{user,catalog}_tools.py` |
| ~~M4~~ | ~~CI & evals~~ **✅ DONE** (thresholds.toml + `--subset` PR gate; e2e side-effect suite through the live API incl. semantic-cache assertions; dedup gray-band eval baseline 12/12 floor 0.75; ci/nightly workflows; deploy.yml inert per ADR-009; ADR-026…029; sabotage-PR proof done, CI 8m11s). Nightly dispatch dry-run pending merge | `evals/*`, `.github/workflows/*`, `evals/thresholds.toml` |
| ~~M5~~ | ~~Memory + full eval harness~~ **✅ DONE** (Postgres sessions via SDK SQLAlchemySession, sqlite stopgap deleted; user_facts inject/extract lifecycle; quality suite (LLM-as-judge, gpt-5); e2e 6→18 flows; retrieval 30→40; wrong-handoff matrix; per-case cost/latency; committed baseline; ADR-030…034) | `app/memory/*`, `app/db/migrations/versions/0002`, hooks in `app/api/routes_chat.py`, `tests/test_memory.py`, `evals/{suite_quality,judge_prompt.md,results/baseline.json}` |
| ~~M6~~ | ~~Observability~~ **✅ DONE** (Langfuse via SDK trace processor, empty-keys no-op; tag schema + eval datasets + trace-id hook; caching A/B via CACHES_DISABLED; cost budget alert; harness-vs-traces cross-check exact; README metrics section; ADR-042…045) | `app/observability/{tracing,costs}.py`, `evals/langfuse_datasets.py`, `scripts/{export_metrics,ab_caching_report}.py`, `docker-compose.langfuse.yml`, `tests/test_observability.py` |
| ~~M7~~ | ~~AWS migration~~ **✂️ DROPPED** (2026-07-07, cut for time — PaaS deploy per DEPLOY.md is the deployment story; first deploy per ADR-009/029 still manual + user-executed) | `DEPLOY.md`, `.github/workflows/deploy.yml` |
| ~~M8~~ | ~~MCP server + Slack + guardrails~~ **✅ DONE** (Socket Mode runner as a pure HTTP client of /chat; fail-closed identity mapping both surfaces; MCP = same plain tools, read/create only, bearer-token→user; injection guardrail flag-then-refile + adversarial fixture; slack suite baseline 5/5·4/5·5/5, floor 0.75; ADR-038…041) | `app/slack/runner.py`, `app/tools/slack_tools.py`, `mcp_server/server.py`, `app/agents/guardrails.py`, `evals/{suite_slack.py,datasets/slack.jsonl}`, `SLACK_SETUP.md` |
| ~~M9~~ | ~~Graph-RAG~~ **✅ DONE** (CMDB `cis`+`dependencies` schema, migration 0003; query_dependency_graph on the incident agent; Postgres-CTE default backend + optional parity-tested Neo4j; flagship three-way eval — RAG F1 0.44–0.64 vs Graph-RAG 1.000; ADR-035…037) | `graph/`, `app/tools/graph_tools.py`, `app/db/migrations/versions/0003`, `evals/{suite_graph.py,datasets/graph.jsonl}` |
| ~~M10~~ | ~~Latency~~ **✅ DONE** (span-attributed: 97–99% of wall time = LLM generation; reasoning effort config-driven — router `minimal`, fulfillment/incident `low`, knowledge `medium` after a measured carve-out (low decorated refusals with the forbidden Sources list → cache leak); probes −23…−67% wall, −24…−53% cost, floors untouched, e2e 18/18; gpt-5-nano router measured & rejected at zero floor margin; ADR-047) | `app/config.py`, `app/agents/{router,knowledge,fulfillment,incident}.py` (model_settings only), `tests/test_agents.py` pins, `evals/suite_slack.py` (stale ADR-046 assert), README "Latency", evidence `ignore/tem/m10_*` |
| ~~M11~~ | ~~Streaming~~ **✅ DONE** (POST /chat/stream — SSE as a second presentation of the FROZEN /chat pipeline via extracted shared helpers; delta/status/final/error, final built from ChatResponse; extraction rides the StreamingResponse; run_streamed sessions/tracing verified live pre-design; TTFT: status ~1.1 s, first token 5–9 s (anatomy: pre-text calls + reasoning are token-free), wall unchanged — stated honestly; e2e 19/19 incl. new stream_parity, slack 5/5, tests 173; multi-intent cache exposure disclosed; ADR-048) | `app/api/routes_chat.py`, `ui/streamlit_app.py`, `tests/test_streaming.py` (+route pin in `tests/test_observability.py`), `evals/{suite_e2e.py,datasets/e2e.jsonl}`, README "Streaming", `design/itsm_ai_system_architecture.*`, evidence `ignore/tem/m11_*` |

## Conventions

- (Historical — no stubs remain; M6 implemented the last one.) Stubs stayed stub-shaped until their milestone, each naming it in a `# TODO(M<n>):` comment.
- Private/local files go in `ignore/` (never committed).
- Don't commit unless the user explicitly asks.
- The `itsm` name in `.env.example`/`docker-compose.yml` is the **database** name, intentionally kept (not the repo name).
